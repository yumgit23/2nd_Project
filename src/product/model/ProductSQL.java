package product.model;

public class ProductSQL {
	static final String PRODUCT_LIST = "select * from pd where pd_type='neck' order by pd_name desc";
	//static final String PRODUCT_DETAIL = "select * from PRODUCT where pd_id =" + pd_id;
}
